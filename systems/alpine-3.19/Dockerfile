FROM alpine:3.19

RUN apk add --no-cache \
    wget \
    autoconf \
    dpkg-dev \
    dpkg \
    file \
    g++ \
    gcc \
    libc-dev \
    make \
    pkgconf \
    re2c \
    tar \
    xz \
    bison \
    pcre-dev \
    libxml2-dev \
    libressl-dev \
    sqlite-dev \
    libpng-dev \
    freetype-dev \
    gnu-libiconv-dev \
    vim \
    cmake \
    git \
    && mkdir -p /opt

WORKDIR /opt
